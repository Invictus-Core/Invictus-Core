name: Generate Stats Images
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: jstrippel/github-profile-summary-cards@main
        with:
          user: Invictus-Core
